import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'przystanki-v2',
  webDir: 'dist/przystanki-v2/browser'
};

export default config;
