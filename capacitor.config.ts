import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pl.kamzago35.przystanki_v2',
  appName: 'Przystanki V2',
  webDir: 'dist/przystanki-v2/browser'
};

export default config;
